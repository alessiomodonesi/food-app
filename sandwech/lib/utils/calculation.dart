/// Calcola la percentuale dato un valore totale e un quantitativo
/// che rappresenta la percentuale che si vuole ottenere
double calcPercentage(double size, double percentage) {
  return (size * percentage) / 100;
}
